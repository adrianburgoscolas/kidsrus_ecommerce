import { francois_one } from "@/app/fonts/fonts"

export default function Pages() {
  return (
    <main className="grow">
      <p className={`text-center text-primary ${francois_one.className}`}>Pages page under construction</p>
    </main>
  )
}
