import { francois_one } from "@/app/fonts/fonts"

export default function PostTypes() {
  return (
    <main className="grow">
      <p className={`text-center text-primary ${francois_one.className}`}>Post Types page under construction</p>
    </main>
  )
}
