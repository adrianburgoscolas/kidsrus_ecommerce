import { francois_one } from "@/app/fonts/fonts"

export default function ShopFeatures() {
  return (
    <main className="grow">
      <p className={`text-center text-primary ${francois_one.className}`}>Shop Features page under construction</p>
    </main>
  )
}
