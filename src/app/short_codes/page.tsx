import { francois_one } from "@/app/fonts/fonts"

export default function ShortCodes() {
  return (
    <main className="grow">
      <p className={`text-center text-primary ${francois_one.className}`}>Short Codes page under construction</p>
    </main>
  )
}
